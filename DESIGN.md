# 空岛求生 - 游戏设计文档

## 概述
基于"空岛求生"小说题材的文字生存游戏。
**架构目标**：文字版 → 2D版只需替换表现层，核心逻辑零修改。

## 世界观
全球人类被传送至浮空群岛世界。每座空岛悬浮于虚空之上，面积有限，资源匮乏。
你需要在空岛上生存、探索、建造、战斗，逐步扩张领地，揭开空岛世界的秘密。

## 核心玩法循环

```
探索岛屿 → 收集资源 → 制作装备/建造设施 → 击败守护者
    ↑                                          ↓
    └──── 融合新岛屿 ← 解锁新区域 ← 击杀成功 ←──┘
```

## 五大系统
1. **生存系统**：饥饿、生命、体力：饥饿、生命、体力
2. **采集系统**：空岛资源点探索与采集
3. **制作系统**：工具、武器、装备、建筑材料
4. **建造系统**：设施建造、领地扩张
5. **战斗系统**：回合制战斗，技能与装备搭配

## 架构设计（v2 - 面向多人+图形化）

```
┌───────────────────────────────────────────────────────┐
│               展示层 (Presentation)                    │
│  TextUI (当前)  ←→  AnimUI(图形版)  ←→  WebUI        │
│  只负责：文字显示 / 动画播放 / 图片展示 / 音效播放     │
│  订阅 DisplayEvent，不做逻辑判断                       │
├───────────────────────────────────────────────────────┤
│               引擎层 (Engine)  ← v2新增                │
│  GameEngine: 接收 InputAction → 分发系统 → 收集事件    │
│  管理 Player 实体、世界状态                            │
├───────────────────────────────────────────────────────┤
│               网络层 (Network)  ← v2新增               │
│  确定性随机 / 输入验证 / 状态快照 / 消息协议            │
│  多人时：客户端发 InputAction，服务器同步 StateSnapshot │
├───────────────────────────────────────────────────────┤
│               系统层 (Systems)                         │
│  CombatSystem  SurvivalSystem  ExploreSystem           │
│  CraftSystem   FishingSystem   DisasterSystem          │
├───────────────────────────────────────────────────────┤
│               核心层 (Core)                            │
│  Player(实体)  EventBus  GameState  World              │
├───────────────────────────────────────────────────────┤
│               数据层 (Data)                            │
│  items.json  recipes.json  islands.json  creatures.json│
└───────────────────────────────────────────────────────┘
```

## v2 核心设计决策

### 玩家实体化
- Player = Stats + Inventory + Equipment + Position
- 装备从背包扫描改为正式槽位（weapon/head/body/accessory）
- 每个 Player 有唯一 ID，支持多人区分

### InputAction 统一操作
- 所有玩家操作封装为 InputAction（可序列化）
- 多人同步只需发送 InputAction（几十字节），不需要同步整个状态
- 操作类型枚举：FISH / GATHER / CRAFT / ATTACK / TRAVEL / ...

### 展示事件分离
- DisplayEvent 用于触发动画/图片/音效
- 文字版忽略 DisplayEvent，图形版订阅后播放对应动画
- 图形是"装饰"，不参与游戏逻辑

### 确定性随机
- DeterministicRandom 替代 random 模块
- 相同种子产生相同序列，支持多人状态同步

## 转换策略
- 所有游戏逻辑在 `core/` 和 `systems/` 中，零UI依赖
- UI通过EventBus接收游戏事件进行显示
- 玩家输入通过 GameEngine.submit_action(InputAction) 提交
- 2D版只需实现新的展示层，订阅 DisplayEvent 播放动画
